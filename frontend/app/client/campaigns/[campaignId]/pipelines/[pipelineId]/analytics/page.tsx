import { SectionPlaceholder } from "@/components/client/section-placeholder";

export default function PipelineAnalyticsPage() {
  return (
    <SectionPlaceholder
      title="Pipeline Analytics"
      description="Per-pipeline conversion/contactability/PTP-rate breakdown, once Pipeline exists to scope campaign_management's analytics by."
      columns={[]}
      note="Pipeline analytics"
    />
  );
}

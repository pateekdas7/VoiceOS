import { EmptyState } from "@/components/ui/empty-state";

export default function ClientSettingsPage() {
  return (
    <EmptyState
      title="Settings"
      description="Genuinely blocked, not just unwired: VoiceProfile (TTS speaker management) and the notifications service ADR-005 designs here are net-new entities with zero backend model/repository/service today -- the same category as Pipeline, not a missing frontend wire onto an existing capability. Tenant profile / integrations-webhooks / API usage-rate-limits have no dedicated backend surface either."
      adrRef="ADR-005 §6.17 / §6.10 / §6.13 / §6.14"
      status="backend-pending"
    />
  );
}

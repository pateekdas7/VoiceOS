# VoiceOS v2 — Sprint Backlog

**Generated:** 2026-06-29 · **Updated:** 2026-07-08 (Sprint-027 complete — Monitoring, Alerting, Logging, Tracing & Disaster Recovery. Full observability stack [Prometheus/Grafana/Alertmanager/Loki/FluentBit/OTel Collector/Jaeger] deployed for real into `voiceos-ops`: 26/26 real VoiceOS Kubernetes targets `up` in Prometheus, 15 Grafana dashboards [6 SLO/ops + 5 governance + 4 security] provisioned and verified, Loki log search by `call_id` verified against a real StructuredLogger line, a real OTLP trace verified end-to-end in Jaeger, Alertmanager routing verified with both a synthetic burn-rate alert and real `NodeDown`/`GPUUnavailable` alerts. `monitoring/gpu_fleet/` [`GPUFleetHealthMonitor`/`ModelWarmupOrchestrator`/`FleetVRAMBudget`] and `src/services/cost_optimizer/`/`src/services/ops_analytics/` all implemented and unit-tested (95%+ coverage on the new code). A real Postgres failover DR drill completed in 4s [target ≤60s] with zero data loss — `infra/dr/DR_DRILL_REPORT_2026-07-08.md`. Full regression suite: 2091 passed/1 skipped (up from 2056, +35 new tests), unchanged before/after the DR drill. 3 real infra bugs found and fixed this sprint: (1) FluentBit's `docker` parser silently failed against this cluster's actual `containerd` CRI plain-text log format — fixed by switching to a `cri` parser; (2) the same apiserver-NAT limitation TT-015 documented for the GPU node also blocks in-cluster pods from reaching `kubernetes.default.svc`, and FluentBit's constant retry against it was found to starve its own Loki delivery — fixed by removing the `kubernetes` filter (see new **TT-015-addendum**); (3) a stale hardcoded Alembic head assertion in `healthcheck.sh` (`0025`→`0026`, same recurring bug class as every prior sprint). New **TT-017** filed (GPU node's STT/LLM/TTS never bound `/metrics`, same class as TT-006 — not fixed, GPU node access needs per-instance approval). See CHANGELOG.md's Sprint-027 entry and `implementation/DONE.md` for full detail. Previously — 2026-07-07 (Sprint-026 now complete on **both Phase 1 and Phase 2** — the original CPU node [216.48.191.142, TT-014] was migrated to a genuinely unrestricted VM [`101.53.141.75`, TT-014 resolved] and the full Helm/Kubernetes Phase 2 deployment executed for real: `kubeadm` v1.36.2 + Calico v3.28.0 CNI, all 26 pods Running/Ready except `gpu-scheduler` [correctly Pending, no GPU node joined — proves K8S-2 is configured exactly right], Guaranteed QoS/PDB/NetworkPolicy-enforcement all validated with real evidence, migration `0026` applied for real, full suite 2056 passed/1 skipped. 8 real bugs found and fixed (Vault/healthcheck pipefail bugs, Redis-auth false-positive, uppercase PriorityClass names, a missing-`{{ }}` NetworkPolicy template bug, a missing ConfigMap `PORT` var, stale migration-head test). GPU node join attempted for full K8S-2 validation — succeeded, but a cross-provider NAT limitation (no shared VPC) blocked Calico; cleanly reverted, GPU inference services unaffected throughout — filed as new **TT-015**. A chart-generator gap [`egressPorts` never covers peer-service HTTP ports] filed as new **TT-016**. See CHANGELOG.md's Sprint-026 Phase 2 entry and `implementation/DONE.md` for full detail. Previously — 2026-07-07 (Sprint-026 complete on Phase 1 — Infrastructure as Code & Kubernetes Architecture: complete Terraform module tree [`infra/terraform/` — network/kms/kubernetes/database/redis/mongodb/object-storage/registry modules + dev/staging/production environments], complete Helm umbrella chart + 25 service sub-charts [`infra/helm/voiceos-platform/`, generated from a shared template via `scripts/helm/generate_service_charts.sh` — every chart has Deployment/Service/ConfigMap/HPA/PDB/NetworkPolicy/ServiceAccount, Guaranteed QoS, GPU taint/toleration only on `gpu-scheduler`], `infra/k8s/` cluster config [namespaces, CRITICAL/HIGH/NORMAL/LOW priority classes, resource quotas, deny-all NetworkPolicy baseline, Kyverno pod-security ClusterPolicies], `src/services/saas_ops/` [`FeatureFlagService`/`FleetRolloutManager`/`TenantDataMigration`/`EntitlementOpsService`, migration `0026`]. Phase 1 fully validated: `terraform validate`/`plan` (dev) 0 errors, `tfsec` 0 HIGH/CRITICAL findings (3 real findings found and fixed — public EKS endpoint, public-IP subnets, missing IMDSv2 — see CHANGELOG.md), `helm lint --with-subcharts` 0 warnings across all 26 charts, `helm template | kubeconform -strict` 180/180 valid manifests, ruff/mypy --strict/`check_boundaries.py` all clean, full local suite 1985 passed/72 skipped (90.42% coverage). **Phase 2 blocked on a genuine infrastructure gap, not attempted as a live deployment:** direct SSH investigation of the CPU node proved it cannot run any Kubernetes distribution (kubeadm or k3s) — it is a capability-restricted notebook-server pod on a shared ML platform (`unshare` returns `Operation not permitted`, `cap_sys_admin`/`cap_net_admin` absent from its capability set), not fixable from inside the pod. See BACKLOG.md's new TT-014 for the full evidence trail. Milestone/epic status unchanged (E7 Production Alpha remains in progress). Previously: Sprint-025 final resolution — both remaining review items from the Part-3 pass now closed: (1) prompt-pin authority moved from `CampaignService.activate()` [optional, bypassable] to `ConversationEngine._require_pinned_prompt()`, enforced on every turn before any inference, proven against the real full pipeline via 3 new tests; (2) Admin Portal authority re-verified as a complete 1:1 match to Sprint-025.md's literal 6-controller spec, no code change needed. CPU node: 1995 passed/31 skipped, infra-validation 27/27 PASS (re-confirmed), 0 leftover rows. **Sprint-025 is now complete against its own spec, Volume 5 Ch13–16, and Volume 4 Ch5/Ch12 — no outstanding architecture questions.** Full detail in CHANGELOG.md's "Final resolution" entry and `implementation/adrs/ADR-002-sprint025-scope-expansion.md` §10. Previously — 2026-07-07 (Sprint-025 gap-fill rounds 1-2 + Part-3 schema extension/cross-platform wiring — additive migration `0025` on top of `0024` [`webhook_delivery_attempts`/`webhook_dead_letter_queue`/`api_key_usage`/`api_rate_limits`/`admin_audit_views` view/`api_keys.expires_at`+`plan_tier`]; AI Config wired into `ConversationEngine.resolve_runtime_config()`; PromptVersioning wired into `CampaignService.activate()`'s pin gate; EventBus wired for exactly-once webhook dispatch; persisted rate limits + dual-window burst handling; full `APIKeyLifecycleService`/`APIKeyAdminController`; `admin_audit_views` query route. CPU node re-validated: 1992 passed/31 skipped, infra-validation 27/27 PASS, `healthcheck.sh` updated and clean. **Milestone M-6 status unchanged (already reached).** Previously — 2026-07-06 (Sprint-025 complete — AdminAPI/TenantAdminController/UserAdminController/CampaignAdminController/BillingAdminController/AuditAdminController/AIConfigAdminController [`src/services/admin_portal/`], PromptVersioningService/ModelConfigService/EvaluationRunService [`src/services/ai_config/`], WebhookService/WebhookDeliveryEngine/WebhookSigner [`src/services/integration_platform/`], PublicAPI/openapi.py/rate_limits.py [`src/services/api_platform/`] all implemented; new repositories `PromptVersionRepository`/`ModelConfigRepository`/`WebhookRegistrationRepository`/`WebhookDeliveryRepository`/`APIKeyRepository`; `APIKeyValidator` gained an optional Postgres-backed fallback (additive); migration `0024` (additive: `prompt_versions`/`campaign_prompt_pins`/`model_configs`/`webhook_registrations`/`webhook_deliveries`/`api_keys`, 49 public tables total); new domain events `PTPBroken`/`CampaignCompleted`; new dependency `pyyaml`; new `api-specs/voiceos-public-v1.yaml` (OpenAPI 3.1) + `scripts/validate_openapi.py`; 1889 passed/0 failed locally (90.61%), 1960 passed/1 skipped on the CPU node (the 1 skip is the pre-existing unrelated Devanagari deferral) — no regressions. `scripts/sprint025_infra_validation.py` 8/8 PASS (prompt hash/immutability, model config campaign-override-wins-over-tenant-default, real signed webhook HTTP round trip, 3-retry→DLQ, Postgres-backed API key resolution + rejection). 2 real-infra-only issues found and fixed [`ModelConfigRepository.upsert()`'s `ON CONFLICT ON CONSTRAINT` targeted a partial index name, not a real constraint, fixed to target `(columns) WHERE <predicate>` directly; infra-validation script's own synthetic non-UUID `campaign_id` against a real UUID FK, fixed by creating a real Campaign row first — same recurring bug class as prior sprints], plus 1 pre-existing unrelated bug found and fixed [stale hardcoded migration-head test assertion, plus a separately-stale `healthcheck.sh` Alembic-head check live since Sprint-024] — see CHANGELOG.md. **Milestone M-6 SaaS Platform Complete reached — Epic E6 closed.** Previously: Sprint-024 complete — BillingService/SubscriptionManager/EntitlementEngine/InvoiceGenerator/PaymentProcessor [`src/services/billing/`], MeteringService/UsageCollector/UsageAggregator/UsageLimitEnforcer [`src/services/metering/`], AnalyticsService/CallAnalytics/CampaignAnalytics/DailyAggregationJob/RealtimeAnalytics [`src/services/analytics/`], ReportingService/ExportService/ReportScheduler [`src/services/reporting/`], BIPlatformService/BIWarehouse/ForecastingEngine/CrossTenantBenchmarking/ExecutiveDashboard [`src/services/bi_platform/`] all implemented; new repositories `InvoiceRepository`/`CallDispositionRepository`/`AnalyticsDailyRepository`/`BIRepository`; new `BillingPolicyPack` (sixth built-in Policy Engine pack, domain="billing") + `PolicyEngineService.check_entitlement()`; migrations `0021`(additive tier/usage-type enum values + `invoices.line_items` JSONB)/`0022`(new `analytics_daily` table)/`0023`(new dedicated `bi_facts` Postgres schema — `dim_tenant`/`fact_daily`); new domain events `STTTranscribed`/`LLMGenerated`/`GPUAllocated`; new dependencies `openpyxl`/`reportlab` (XLSX/PDF export); 1862 passed/0 failed locally, 1927 passed/7 skipped on the CPU node (92.20%, all 7 skips are the pre-existing MongoDB-credential gap [6] + Devanagari deferral [1]) — no regressions. `scripts/sprint024_infra_validation.py` 11/11 PASS (GROWTH subscription creation, usage event capture via real EventBus → Postgres, idempotency, GROWTH call-minute limit enforcement, invoice generation matching usage aggregate, campaign ptp_rate=0.30, scheduled DailyAggregationJob, BIWarehouse.refresh() into bi_facts, non-zero forecast, anonymized cross-tenant benchmark, executive summary all-KPIs-present); `scripts/validate/usage_metering.py`/`usage_limit.py` both PASS. 2 real-infra-only issues found and fixed [non-UUID tenant_id values with no backing `tenants` row in new integration tests/validation script — same recurring bug class as prior sprints; a hand-written Redis Protocol rejecting the real `redis.Redis` client's wider `.set()` return type, retyped to `Any`] — see CHANGELOG.md. Previously: Sprint-023 complete — CampaignService/CampaignLifecycle/AudienceSelector/ScheduleEngine/RetryPolicyEngine/ABTestingFramework/CallDispatcher [`src/services/campaign_management/`], SkillsBasedRouter/LiveTransferService/SupervisorService/AgentScreenContext [`src/services/contact_center/`], HITLQueue/SLAEnforcer/HumanReviewAPI/OverrideLogger/HITLDashboard [`src/services/hitl/`] all implemented; new repositories `CampaignAudienceRepository`/`CampaignResultRepository`/`HITLQueueRepository`/`HITLDecisionRepository`; migration `0020` reworks `campaigns.status` to the real 7-state lifecycle + adds `campaign_audiences`/`campaign_results`/`hitl_queue`/`hitl_decisions`; `ConversationEngine` gains optional `contact_center_service`+`escalate_call()`/`campaign_id_for_call()`; `HITLQueue` wired into `GovernanceLayer`/`AIGovernanceService` via a new `HumanOversightRouterPort` Protocol; 1769 passed/67 skipped locally (91.38%), 1835 passed/1 skipped on the CPU node (91.84%, the 1 skip is the pre-existing unrelated Devanagari deferral) — no regressions. `scripts/sprint023_infra_validation.py` 10/10 PASS (campaign lifecycle DRAFT→REVIEW→APPROVED→ACTIVE + invalid-transition raises, RBI 21:00/3-calls-today both return None via the real Policy Engine, 100-dispatch A/B split within tolerance, HITL enqueue→resolve round-trip durable across a fresh repository instance, HITL SLA breach detection, live-transfer AgentScreenContext includes transcript+summary, supervisor monitor read-only + barge-in mutes AI). 2 real-infra-only issues found and fixed [`hitl_queue.call_id` was typed UUID but ScheduleEngine builds synthetic non-UUID call identifiers, retyped to TEXT; stale hardcoded migration-head test assertion] — see CHANGELOG.md. Previously: Sprint-022 complete — CustomerService/PartyService/CustomerContextAssembler/CustomerImporter [`src/services/crm/`], LoanAccountService/EMIScheduleService/PromiseToPayService/SettlementService/CallbackScheduler/EscalationWorkflow [`src/services/collections/`] all implemented; new repositories `EMIScheduleRepository`/`SettlementRepository`/`CallbackRepository`/`EscalationRepository`/`PartyRepository`; migration `0019` adds `settlements.approved_by`/`authorized_at` (additive) — `settlements`/`callback_requests`/`escalation_records` tables already existed from migration 0007; `ConversationEngine` gains optional `context_assembler`+`start_call()`/`end_call()` (CustomerContext assembled once per call, RI-5); 1696 passed/63 skipped locally (92.02%), 1752 passed/7 skipped on the CPU node (92.40%, 7 skips = pre-existing TT-008 Mongo gap) — no regressions. `scripts/sprint022_infra_validation.py` 8/8 PASS (CustomerContext assembly correct amounts/DPD=30, immutability, RI-5 blocks unauthorized source, PTP 10-concurrent → 1 row, PTP audit persisted, settlement offer→accept→authorize→disburse, latency 0.60ms). 2 real-infra-only issues found and fixed [stale hardcoded migration-head test assertion; validation script's audit_log cleanup attempt correctly rejected by the immutability trigger] — see CHANGELOG.md. Previously: Sprint-021 complete — TenantService/TenantLifecycle/TenantProvisioner/IsolationProfileManager/TenantSuspender/TenantDeleter [`src/services/tenant_management/`], OrgHierarchy/OrgService [`src/services/org_management/`], InvitationService/UserService/SSOIntegration stub [`src/services/user_management/`] all implemented; migration `0018` reworks `tenants.status` to the real 7-state lifecycle + adds `invitations`/`sso_config`; new `SaaSPolicyPack` domain="tenant" rule; 1661 passed/60 skipped locally (92.35%), 1720 passed/1 skipped on the CPU node (92.70%) — no regressions. `scripts/sprint021_infra_validation.py` 7/7 PASS; walking-skeleton e2e 6/6. 2 real-infra-only issues found and fixed [a UUID-typed `assigned_by` column bug in `TenantProvisioner`'s default-admin role assignment; a `RedisClient`-vs-raw-client mismatch in the validation script itself] — see CHANGELOG.md. First sprint of Epic E6 (SaaS Platform). Previously: Sprint-020 complete — PIIDetector/PIIRedactor/PIITokenizer, hash-chained AuditLogger/AuditVerifier [migration `0017`, additive: `audit_log.seq/prev_hash/hash` + `pii_tokens`], API/runtime security [APIRateLimiter/InputValidator/SecurityHeaders/CORSPolicy/ContainerSecurityPolicy/NetworkPolicy], AI safety [ContentModerator/PromptInjectionDetector/AIOutputValidator/HumanOversightRouter, wired into GovernanceLayer + ConversationEngine], ComplianceMonitoring + IncidentResponse [DPDP 72h timer] all implemented; `StructuredLogger` now unconditionally redacts PII on every log path; 1627 passed/57 skipped locally (93.34%), 1679 passed/1 skipped/4 failed [documented Mongo-credential gap, TT-008] on the CPU node (93.38%) — no Sprint-020 regressions. `scripts/sprint020_infra_validation.py` 12/12 PASS; DR scripts (`audit_chain.py`/`consent_gate.py`) both PASS; walking-skeleton e2e 6/6. 2 real-infra-only issues found and fixed during Phase 2 [stale migration-head test assertion; a `healthcheck.sh` `REDIS_URL`-clobbering bug plus a credential-in-print leak across 6 scripts, see TT-007] — see CHANGELOG.md. **Milestone M-5 Compliance & Security Complete reached.** Previously: Sprint-019 complete — SecretsManager [self-hosted Vault KV v2], EnvelopeEncryption [Vault Transit KMS, DEK/KEK], CryptoShredder, PrivacyEngine/DataMinimizer/DataErasureJob/RetentionScheduler implemented; `customers`/`customer_contacts`/`customer_addresses` gained field-level `_encrypted` columns [migration `0016`, additive]; `CustomerRepository` optionally encrypts/decrypts transparently; `scripts/check_secrets.py` wired into CI as a new blocking gate; ruff/mypy --strict [455 files]/boundaries/check_secrets.py all clean; 1570 passed/57 skipped locally (93.31%), 1626 passed/1 skipped on the CPU node against real Postgres/Redis/MongoDB/Vault (93.35%) — no regressions. Real self-hosted Vault (KV+Transit) provisioned from zero on the CPU node; Redis/MongoDB auth enforced (net-new scope, separate from the already-resolved TT-002). `scripts/sprint019_infra_validation.py` 9/9 PASS; regression 1626/1627. 3 real-infra-only issues found and fixed during Phase 2 [container `cap_ipc_lock` blocking Vault's exec; a base64 password breaking URL construction; 3 pre-existing tests stale against the new migration/schema] — see CHANGELOG.md. Previously: Sprint-018 complete — AuthService [JWT/OIDC/mTLS/API-key], AuthzService [RBAC+ABAC+TenantIsolationGuard+JITPrivilege], AIGovernanceService [GovernanceLayer+LawOfAuthorityChecker] implemented; AI Governance gate wired as a **mandatory** [not optional] constructor param on ConversationEngine; self-managed mTLS PKI provisioned on the CPU node. Milestone M-4 Reliability Complete reached [Sprint-016]. TT-006 remains open — no service has a standalone HTTP listener yet for the new `/health/*`/`/metrics` endpoints.)
**Total Sprints:** 34
**Completed:** 27 (Sprint-027 complete — full observability stack + DR drill validated for real; see TT-015-addendum, TT-017)
**In Progress:** 0
**Pending:** 7

Legend: `⬜ Pending` · `🟡 Current` · `✅ Done` · `🔴 Blocked`

---

## Epic E1 — Foundation & Engineering Infrastructure

| Sprint | Title | Status | Depends On |
|---|---|---|---|
| Sprint-001 | Repository Scaffolding, Contracts & Invariants | ✅ **DONE** (2026-06-30) | — |
| Sprint-002 | Event Contracts & Data Models | ✅ **DONE** (2026-06-30) | Sprint-001 |
| Sprint-003 | Testing Infrastructure, CI/CD Pipeline & Developer Tooling | ✅ **DONE** (2026-06-30) | Sprint-001, Sprint-002 |

---

## Epic E2 — Core Voice Runtime

| Sprint | Title | Status | Depends On |
|---|---|---|---|
| Sprint-004 | Media Gateway | ✅ **DONE** (2026-06-30) | Sprint-001, Sprint-002, Sprint-003 |
| Sprint-005 | Audio Session Manager | ✅ **DONE** (2026-06-30) | Sprint-004 |
| Sprint-006 | Audio Preprocessing Pipeline | ✅ **DONE** (2026-06-30) | Sprint-005 |
| Sprint-007 | VAD & Endpointing | ✅ **DONE** (2026-06-30) | Sprint-006 |
| Sprint-008 | GPU Scheduler | ✅ **DONE** (2026-06-30) | Sprint-001, Sprint-002, Sprint-003 |

---

## Epic E3 — Conversation Intelligence

| Sprint | Title | Status | Depends On |
|---|---|---|---|
| Sprint-009 | STT, LLM & TTS Adapter Services | ✅ **DONE** (2026-07-03) | Sprint-008 |
| Sprint-010 | Intelligence Engines — Perception Layer | ✅ **DONE** (2026-07-03) | Sprint-001, Sprint-002, Sprint-009 |
| Sprint-011 | Intelligence Engines — Decision Layer | ✅ **DONE** (2026-07-03) | Sprint-010 |
| Sprint-012 | Conversation Orchestration — Walking Skeleton | ✅ **DONE** (2026-07-04) | Sprint-009, Sprint-010, Sprint-011 |

---

## Epic E4 — Reliability Infrastructure

| Sprint | Title | Status | Depends On |
|---|---|---|---|
| Sprint-013 | Event Bus & Redis Architecture | ✅ **DONE** (2026-07-04) | Sprint-001, Sprint-002, Sprint-003 |
| Sprint-014 | Persistent Storage — Schemas & Migrations | ✅ **DONE** (2026-07-04) | Sprint-002, Sprint-003 |
| Sprint-015 | State Persistence, Crash Recovery & Idempotency | ✅ **DONE** (2026-07-04) | Sprint-013, Sprint-014 |
| Sprint-016 | Concurrency, Circuit Breakers, Health & Observability | ✅ **DONE** (2026-07-04) | Sprint-013, Sprint-014, Sprint-015 |

---

## Epic E5 — Compliance & Security

| Sprint | Title | Status | Depends On |
|---|---|---|---|
| Sprint-017 | Policy Engine & Regulatory Compliance | ✅ **DONE** (2026-07-04) | Sprint-001, Sprint-002, Sprint-013, Sprint-014 |
| Sprint-018 | Authentication, Authorization/RBAC & AI Governance | ✅ **DONE** (2026-07-04) | Sprint-017 |
| Sprint-019 | Secrets Management, Encryption & Privacy Architecture | ✅ **DONE** (2026-07-05) | Sprint-017, Sprint-018 |
| Sprint-020 | PII Protection, Audit, API Security & AI Safety | ✅ **DONE** (2026-07-05) | Sprint-018, Sprint-019 |

> **TT-002 (Redis Production Hardening & EventBus Recovery) was resolved 2026-07-04** ahead of schedule as a dedicated hardening task — see Technical Debt below. Sprint-019 cross-referenced this (did not redo it) and instead closed the separate, still-open Redis/MongoDB **auth** gap (`requirepass`/`--auth`) as part of its own secrets/encryption scope.

---

## Epic E6 — SaaS Platform

| Sprint | Title | Status | Depends On |
|---|---|---|---|
| Sprint-021 | Multi-Tenancy, Tenant Lifecycle & User Management | ✅ **DONE** (2026-07-06) | Sprint-018, Sprint-020 |
| Sprint-022 | CRM & Loan/Collections Management | ✅ Done | Sprint-021, Sprint-014, Sprint-015 |
| Sprint-023 | Campaign Management & Contact Center Platform | ✅ **DONE** (2026-07-06) | Sprint-022 |
| Sprint-024 | Billing Platform, Usage Metering & Analytics | ✅ **DONE** (2026-07-06) | Sprint-021, Sprint-022 |
| Sprint-025 | Admin Portal, AI Configuration & Integration Platform | ✅ Done | Sprint-021, Sprint-023, Sprint-024 |
| Sprint-032 | Enterprise Platform: SSO, SCIM & Multi-Region | ⬜ Pending | Sprint-025, Sprint-021, Sprint-018, Sprint-019, Sprint-020, Sprint-026 |
| Sprint-033 | Workflow Automation & Customer Success Platform | ⬜ Pending | Sprint-025, Sprint-013, Sprint-022, Sprint-023, Sprint-024, Sprint-032 |

---

## Epic E7 — Production Alpha

| Sprint | Title | Status | Depends On |
|---|---|---|---|
| Sprint-026 | Infrastructure as Code & Kubernetes Architecture | ✅ **DONE — Phase 1 AND Phase 2** (2026-07-07; real K8s deployment on migrated VM, TT-014 resolved) | Sprint-016, Sprint-020, Sprint-025 |
| Sprint-027 | Monitoring, Alerting, Logging, Tracing & Disaster Recovery | ✅ **DONE** (2026-07-08; full observability stack deployed for real, DR drill 4s RTO) | Sprint-026 |
| Sprint-028 | Performance Validation, Load Testing, Pen Test & Production Alpha Deploy | 🟡 **CURRENT** | Sprint-026, Sprint-027, Sprint-022, Sprint-023 |

> **TT-002 was resolved 2026-07-04** (see Technical Debt below) — Sprint-027's full Disaster Recovery scope (multi-AZ failover, PITR, RTO/RPO validation, chaos engineering) remains as specified; the Redis persistence/eviction/EventBus-recovery portion no longer needs to be carried into this sprint.
>
> **TT-014 (Sprint-026) RESOLVED 2026-07-07:** the CPU node was migrated to a genuinely unrestricted VM (`101.53.141.75`) and a real `kubeadm`+Calico Kubernetes cluster now exists and runs the full Helm-deployed application tier. Sprint-027's real-infrastructure validation (Prometheus/Grafana/Alertmanager/OTel deployment) can now target this real cluster. Note **TT-015**: the GPU node is not (and, per a genuine cross-provider networking limitation, currently cannot be) joined to this cluster — Sprint-027 should account for that if any GPU-side monitoring assumes in-cluster GPU node membership.

---

## Epic E3-Extended — Conversation Intelligence (Offline Learning)

| Sprint | Title | Status | Depends On |
|---|---|---|---|
| Sprint-034 | Conversation Learning Layer | ⬜ Pending | Sprint-010, Sprint-012, Sprint-029 |

---

## Epic E8 — Founder Validation → Pilot → Production Release

| Sprint | Title | Status | Depends On |
|---|---|---|---|
| Sprint-029 | Founder Validation | ⬜ Pending | Sprint-028 |
| Sprint-030 | Pilot Deployment | ⬜ Pending | Sprint-029 |
| Sprint-031 | Production Release | ⬜ Pending | Sprint-030, Sprint-032, Sprint-033, Sprint-034 |

---

## Technical Debt

| ID | Title | Severity | Tracked Since | Description |
|---|---|---|---|---|
| TT-001 | Veena TTS Serving Layer — Batch→Streaming | **RESOLVED (server-level)** | Sprint-012 | Phase 2 first-audio p95=8730ms. Root cause: HF `model.generate()` + buffered `Response`. **Sprint-012 Phase 3 fix (2026-07-04, ADR-001):** `_SNACTokenStreamer` + vLLM `AsyncLLMEngine` + 28-token sliding-window SNAC decode + `FastAPI StreamingResponse` + pipeline `VeenaAdapter`. **Results:** TTS server TTFA p95=873ms ✅ (target 1,500ms); Transfer-Encoding: chunked ✅; true streaming ✅. Veena AI FP16 retained. |
| TT-001-residual | VeenaAdapter Pipeline Concurrency — Sequential Clause Synthesis | Medium | Sprint-012 Phase 3 | Walking skeleton pipeline p95=6,048ms (target p95 ≤ 1,500ms). Root cause: `_yield_clause()` awaits TTS synthesis in the outer LLM-text-consumer loop — LLM and TTS run sequentially rather than concurrently. Fix requires `asyncio.create_task()` for concurrent LLM+TTS tasks, a shared queue between them, and careful backpressure. Targeted for a future sprint after Reliability (E4) is complete. No action in Sprint-013. |
| TT-002 | Redis Production Hardening & EventBus Recovery — Persistence & Eviction Policy | **RESOLVED** | Sprint-013; resolved 2026-07-04 (dedicated hardening task) | **Root cause (proven, not assumed):** (1) the running Redis process on the CPU node had been started with a bare `redis-server 127.0.0.1:6379` command line — bypassing `/etc/redis/redis.conf` entirely — so `appendonly no`/`maxmemory-policy noeviction` were compiled-in defaults regardless of the config file's contents; (2) with AOF disabled, any keyspace-clearing event (restart, flush, session reset) was unrecoverable, and the periodic RDB autosave then permanently persisted the resulting empty state; (3) no long-running VoiceOS consumer process exists yet on this node (services remain library classes until Sprint-026 K8s/Helm) to invoke `Consumer.__init__()`'s already-idempotent `ensure_consumer_group()` call — so recovery depended entirely on a human re-running a one-shot `redis-cli XGROUP CREATE` by hand after every Redis restart, which is why the group kept disappearing across sessions. **Fix:** `/etc/redis/redis.conf` hardened (`appendonly yes`, `appendfsync everysec` — already present, `maxmemory-policy volatile-ttl`) and Redis restarted via the proper init script (`service redis-server restart`) so the config actually loads; `scripts/eventbus_recovery.py` — a canonical, idempotent, race-safe "detect + recreate" wrapper around the same `EventBus.ensure_consumer_group()` — wired into `restore.sh` (replacing an inconsistent raw `redis-cli` one-liner that also used the wrong stream-offset `$` instead of `0`) and `healthcheck.sh` (self-heals on detection instead of merely failing); `bootstrap.sh` bakes the hardening into fresh-node provisioning. **Validated:** a real publish→consume→graceful-Redis-restart cycle proved the stream and consumer group now survive intact (no self-heal even needed) — 3/3 events preserved, `pending=0`, correct `last-delivered-id`, replay and DLQ both functional; Postgres/MongoDB service restarts also verified clean; full regression (1304 passed/1 skipped) green after all three infra restarts. A true full-OS/pod reboot of the CPU node was not performed — it is the only available node and the risk of losing SSH access outweighed the incremental evidence over the already-proven service-level restart tests (user-approved substitution). See `CPU_NODE_STATE.md` §7.1/§18 and `implementation/DONE.md` for full detail. |
| TT-003 | Documentation gaps predating Sprint-013 | **RESOLVED** | Discovered Sprint-013; resolved 2026-07-04 (dedicated cleanup task) | Sprint-012 had no `CHANGELOG.md` entry; `CPU_NODE_STATE.md`/`GPU_NODE_STATE.md` "Last updated" headers lagged behind the sprints that actually touched those nodes; `ROADMAP.md` still said "Implementation not yet started"; `BACKLOG.md` summary counts were stale. **Fixed:** Sprint-012 CHANGELOG.md entry reconstructed from DONE.md/PROJECT_STATUS.md/ADR-001; CPU_NODE_STATE.md backfilled with Sprint-011/012 deployed services, corrected Postgres migration status (29 tables applied, not "not yet applied"), added httpx to the package inventory; GPU_NODE_STATE.md backfilled with the Sprint-012 Phase 3 ADR-001 vLLM streaming TTS deployment (was still describing Sprint-009 Phase 2 batch inference as current); ROADMAP.md status line corrected; BACKLOG.md counts corrected. See CHANGELOG.md and each node-state doc's "Last updated" header for details. |
| TT-004 | Pending operational validation — full CPU node reboot / disaster recovery test | Low | TT-002 (2026-07-04) | TT-002's DR validation substituted independent per-service restarts (Redis/Postgres/MongoDB) plus a full regression run for a true full-OS/pod reboot of the CPU node, since it is the only environment available and a failed reboot risked permanently losing SSH access (no confirmed IP persistence across a restart) — a user-approved deviation, not a skipped requirement. **Action needed:** when a staging node, a disposable clone, or a snapshot/restore-capable environment becomes available (naturally arrives with Sprint-026 IaC/K8s or Sprint-027 DR work), perform a genuine full node reboot and confirm: Redis/Postgres/MongoDB/EventBus all come back automatically, consumer groups are present (persisted or auto-recovered), and the walking skeleton + full regression suite pass with zero manual intervention. Not blocking — the two-layer fix already validated (AOF persistence + `scripts/eventbus_recovery.py` self-heal) covers the actual mechanism being tested; this item closes the remaining gap between "service restart" and "whole-node restart" evidence. |
| TT-005 | `deployment/cpu/healthcheck.sh`/`restore.sh` `REDIS_HOST`/`POSTGRES_HOST` defaults don't resolve on this node | Low | Sprint-015 (2026-07-04) | Both scripts fall back to Docker/K8s-Service-style hostnames (`${REDIS_HOST:-redis}`, `${POSTGRES_HOST:-postgres}`) matching `.env.example`'s intended Sprint-026 production values — but this CPU node runs Redis/Postgres as bare native processes on `127.0.0.1`, so a bare `bash healthcheck.sh` silently reports both as down even when healthy. Discovered running `healthcheck.sh` standalone during Sprint-015 Phase 2 (every prior sprint's documented health checks use manual individual commands, not the script, which is why this was never caught). Workaround used this sprint: export `REDIS_HOST=localhost POSTGRES_HOST=localhost` before invoking either script. **Action needed:** add a real `deployment/cpu/.env` (node-appropriate values, not `.env.example`'s K8s-DNS placeholders) sourced by both scripts, or give the scripts a bare-metal-aware default. Natural fit for Sprint-026 IaC work, since that's also when the K8s-DNS defaults actually become correct. Not blocking — no functional impact on Sprint-015's own deliverables, all of which were validated with the correct env vars exported. |
| TT-006 | `/health/live`, `/health/ready`, and `/metrics` are implemented but not bound to a live listener on any service | Low | Sprint-016 (2026-07-04) | `create_health_app()` (Starlette ASGI app) and per-service `REDMetrics`/Prometheus counters are implemented and unit-tested (`starlette.testclient.TestClient`), but every VoiceOS service remains a library class with no standalone HTTP server process (CPU_NODE_STATE.md §8.1) — there is nothing for Kubernetes liveness/readiness probes or a Prometheus scrape target to hit yet. Same root cause as the GPU-adapter circuit breakers only being exercised against mocks in Sprint-016 Phase 2 (no process to kill). **Action needed:** once Sprint-026 gives each service a real ASGI/uvicorn process, bind `create_health_app()` and a `/metrics` Prometheus exporter to each one and re-run the Sprint-016 Phase 2 Kubernetes-probe/Grafana-rendering validations that were deferred this sprint. Not blocking — the libraries themselves are fully implemented, tested, and ready to bind; Prometheus/Grafana/Jaeger deployment is independently tracked for Sprint-027 (CPU_NODE_STATE.md §13). |
| TT-007 | `healthcheck.sh` `REDIS_URL`-clobbering bug + credential-in-print leak across 6 validation scripts | **RESOLVED** | Discovered and resolved same session, Sprint-020 (2026-07-05) | `deployment/cpu/healthcheck.sh` line 60 unconditionally rebuilt `REDIS_URL` with no password and no `${REDIS_URL:-...}` fallback guard (unlike every other `REDIS_URL`/`POSTGRES_DSN` construction in the same script) — harmless before Sprint-019 (no Redis auth existed), a live failure afterward for any code path relying on the exported var (EventBus self-heal, Policy Engine cache-warm). Separately, `scripts/validate/consent_gate.py` (new) and five pre-existing scripts (`rbi_calling_hours.py`, `sprint013/015/016/017/018_infra_validation.py`) printed the full `REDIS_URL` including embedded credentials in a "connected to Redis" log line — harmless pre-Sprint-019, a real credential-exposure risk after. Both classes found running Sprint-020 Phase 2 for real (the first Phase 2 run to exercise the self-heal path and pass a password-bearing `REDIS_URL` through these scripts). **Fixed:** `healthcheck.sh` now respects a pre-set `REDIS_URL` and embeds `REDIS_PASSWORD` in its fallback construction; all 6 scripts' print statements redacted to omit the URL. See CHANGELOG.md's Sprint-020 entry. |
| TT-008 | MongoDB integration tests / `healthcheck.sh` MongoDB checks need a Mongo credential this session didn't have | Low | Sprint-020 (2026-07-05) | `tests/integration/test_mongodb_connectivity.py` (4 tests) and `healthcheck.sh`'s MongoDB-auth/index checks fail on the CPU node — not a Sprint-020 regression, MongoDB has required `--auth` since Sprint-019 and this session was given Postgres/Redis credentials only, not MongoDB's. **Action needed:** obtain the MongoDB credential (or reset it, same as the Postgres/Redis pattern this sprint) and re-run `pytest tests/integration/test_mongodb_connectivity.py` + `healthcheck.sh` to confirm clean. Not blocking Sprint-020's own scope (PII/audit/API-security/AI-safety/compliance-monitoring/incident-response — none touch MongoDB). |
| TT-009 | `deployment/cpu/restore.sh` required a nonexistent Kubernetes/Helm deployment (`infra/k8s/*.yaml`, `infra/helm/voiceos-platform` — never existed anywhere in this repo); `deployment/gpu/restore.sh`/`model_manifest.yaml` referenced a nonexistent `validate_latency.py` | **RESOLVED** | Discovered during a post-Sprint-020 full reproducibility audit (explicit user request, 2026-07-05); fixed same session | **Root cause (proven against the live CPU node, not assumed):** `restore.sh` called `check helm` and applied `${APP_DIR}/infra/k8s/namespaces.yaml`/`priority-classes.yaml`/`resource-quotas.yaml` plus a Helm chart at `${APP_DIR}/infra/helm/voiceos-platform` — none of these paths have ever existed in this repo, and Helm has never been installed on the CPU node (confirmed live: `helm: command not found`; `kubectl` present but no working cluster context; CPU_NODE_STATE.md §6 has said "Helm is NOT yet installed — planned for Sprint-026" since Sprint-013). Every sprint's real Phase 2 deployment (Sprint-013 through Sprint-020) was performed by hand — venv + `pip install -e .` + `alembic upgrade head` + the individual scripts documented in CPU_NODE_STATE.md — never by running `restore.sh` end-to-end, which is why an unrunnable script went uncaught for 8 sprints. Separately, `deployment/gpu/restore.sh` (line ~161) and `model_manifest.yaml`'s `post_restore_validation.commands` have referenced `deployment/validate_latency.py` since Sprint-009; the file never existed (GPU-side latency numbers in DONE.md were always measured by ad hoc, uncommitted one-off commands). **Fixed:** `deployment/cpu/restore.sh` rewritten to do exactly what has actually been validated on this node every sprint (venv/pip/alembic/Mongo-indexes/Redis-persistence-check/EventBus-recovery/mTLS-PKI/Vault-provisioning/PII-backfill/import-check/healthcheck.sh/regression pytest) with no Kubernetes/Helm dependency — that section will be reintroduced once Sprint-026 actually builds the K8s/Helm deployment it assumed. `deployment/gpu/validate_latency.py` written for real (HTTP latency probes against STT `/transcribe`, LLM `/v1/chat/completions` streaming TTFT, TTS `/synthesize` streaming time-to-first-chunk, checked against `model_manifest.yaml`'s `latency_target_ms` budgets) — not yet exercised against the live GPU node this session (no GPU service call was made; see CHANGELOG.md's audit entry). **Verified:** CPU node's actual current state (migration head `0017`, 36 tables, Vault initialized/unsealed, Redis/MongoDB auth enforced, mTLS certs, Vault scripts present both in-repo and on-node) independently confirmed live via SSH — nothing found configured on the CPU node that isn't captured in a committed script. GPU node (once a fresh SSH endpoint was supplied mid-audit): UUID/services/health/models/systemd units all confirmed matching GPU_NODE_STATE.md; `deployment/gpu/validate_latency.py` (new) deployed and run for real against the live STT/LLM/TTS services — see TT-010 for what it found. |
| TT-010 | TTS `/synthesize` time-to-first-chunk measured at 914–2974ms today — well above both `model_manifest.yaml`'s 300ms target and the Sprint-012 Phase 3 (ADR-001) documented server-level TTFA p95 of 873ms | Medium | Discovered 2026-07-06, during the post-Sprint-020 reproducibility audit (first repeatable, committed measurement since Sprint-012 Phase 3 — TT-009's new `validate_latency.py`) | Three consecutive direct calls to the live GPU node's TTS server (`http://localhost:8200/synthesize`, text="health check", speaker=kavya) measured 914ms, 2702ms, and 1734ms time-to-first-chunk — both substantially higher and far more variable than the 873ms p95 recorded in GPU_NODE_STATE.md §8.1/DONE.md's Sprint-012 Phase 3 entry. `nvidia-smi` at measurement time showed 0% GPU utilization and only the three expected VoiceOS processes resident (STT/TTS/vLLM, matching their documented VRAM footprints) — ruling out GPU contention or an unexpected competing process as the cause. **Not investigated further this session** (root-causing a performance regression is outside a reproducibility audit's scope, and this GPU node is shared/production-adjacent — poking at the running inference server without a plan risks disrupting it). **Action needed:** re-run `python3 deployment/gpu/validate_latency.py --test tts` a larger number of times (ideally with the exact walking-skeleton-style clause text used in the original Sprint-012 measurement) to establish whether this is a real regression (driver/CUDA update, model/weights drift, disk cache cold from node idle, vLLM KV-cache eviction interacting with the SNAC decode step, etc.) or a measurement-methodology difference (the original 873ms was measured via a purpose-built Sprint-012 Phase 3 script now lost to history, not this new one) — then decide whether TT-001-residual's fix (concurrent LLM+TTS tasks) needs to move up in priority. |
| TT-011 | One orphaned Vault Transit KEK left behind by a Sprint-021 infra-validation run | Low | Sprint-021 (2026-07-06) | `scripts/sprint021_infra_validation.py`'s `test_tenant_provisioner_creates_kek`-equivalent check creates a real Vault Transit key (`tenant-<validation-uuid>`) to prove `TenantProvisioner`/`KMSClientProtocol.ensure_kek()` works against live infrastructure, then deletes its Postgres tenant row — but destroying the KEK itself is a separate, irreversible secret-deletion action this session did not have standing authorization to perform unilaterally. Harmless: no real customer data was ever encrypted with it, and the KEK is not referenced by anything (the tenant row that would reference it via `tenant-<id>` naming was deleted). **Action needed:** `vault write transit/keys/tenant-<uuid>/config deletion_allowed=true && vault delete transit/keys/tenant-<uuid>` (the exact `uuid` is printed in the sprint021_infra_validation.py output/CHANGELOG.md), or simply leave it — a stray key with no referencing tenant row is inert. Not blocking. |
| TT-013 | ~16 orphaned `hitl_queue` rows left behind by Sprint-023's own integration-test/validation-script runs against the real CPU node Postgres | Low | Sprint-023 (2026-07-06) | `tests/integration/services/test_hitl_sla.py` (each test creates a fresh random `tenant_id` via `uuid.uuid4()` and never deletes its own rows — same non-cleanup pattern several prior sprints' integration tests already follow) and earlier runs of `scripts/sprint023_infra_validation.py` before its cleanup logic was fixed both left `PENDING`/`RESOLVED` rows in `hitl_queue`/`hitl_decisions`. Harmless — no real HITL usage exists yet on this brand-new feature (no `AIGovernanceService` in production has `HITLQueue` wired as its `human_oversight_router` yet), and the rows don't affect any test's correctness (each test/run uses its own unique `tenant_id`, so nothing collides). A cleanup `DELETE` was attempted and correctly declined by the auto-mode safety classifier (no narrow tenant-scoped predicate available for a *broad* cleanup query) — deleting each of the ~16 individual `tenant_id`s one at a time was judged not worth the round-trips for inert rows. **Action needed:** either leave it (inert), or run `DELETE FROM hitl_decisions WHERE tenant_id = '<uuid>'; DELETE FROM hitl_queue WHERE tenant_id = '<uuid>';` per orphaned `tenant_id` (list via `SELECT DISTINCT tenant_id, enqueued_at FROM hitl_queue ORDER BY enqueued_at;`) the next time a human operator has hands-on access to this node. Not blocking — `hitl_queue_depth` will read non-zero until cleaned, but this is cosmetic (Prometheus scraping doesn't exist yet either, TT-006). |
| TT-014 | No real Kubernetes cluster exists anywhere to deploy Sprint-026's Helm umbrella chart to — the CPU node cannot run one, and this is a hard platform-level block, not a missing-tool gap | **RESOLVED** | Sprint-026 (2026-07-07); resolved same day via CPU node migration | Deepens TT-009. The original CPU node (`216.48.191.142`) was a capability-restricted notebook-server pod on a shared multi-tenant ML platform (`unshare` → `Operation not permitted`, `cap_sys_admin`/`cap_net_admin` absent) — neither `kubeadm` nor `k3s` could ever run there, confirmed by direct investigation (see the original finding, preserved below). **Resolution:** the user provided a genuinely unrestricted VM (`101.53.141.75`, KubeVirt-backed KVM guest, confirmed via `unshare`/capability-bounding-set/real `systemd`) and directed a full production migration to it. `kubeadm` v1.36.2 + Calico v3.28.0 now run for real on this VM, with the complete `voiceos-platform` Helm umbrella chart deployed — 26/26 pods Running/Ready (except `gpu-scheduler`, correctly `Pending` pending TT-015). Full detail in `implementation/DONE.md`'s Sprint-026 "Phase 2" section and CHANGELOG.md. The old node (`216.48.191.142`) remains untouched pending a separate decommission decision. *(Original finding, for the record: the pod's capability set explicitly excluded `cap_sys_admin`/`cap_net_admin`/`cap_sys_module`/`cap_sys_ptrace`/`cap_ipc_lock`; `unshare --mount --uts --ipc --net --pid --fork` failed; not fixable from inside the pod, only by the platform operator or by using different compute — which is exactly what happened.)* |
| TT-015 | GPU node cannot be joined to the new Kubernetes cluster as a worker — cross-provider NAT with no shared VPC blocks Calico/Service-ClusterIP traffic | Medium | Sprint-026 (2026-07-07) | The GPU node (`217.18.55.96`) and the new CPU/control-plane VM (`101.53.141.75`) are hosted by different cloud providers with no shared private network. `kubeadm join` itself succeeds (after opening the VM's firewall on port 6443 and regenerating the apiserver cert with the public IP as an extra SAN — both done, live, user-approved), but the control plane advertises `10.0.2.2` — an address NAT-internal to the VM's hypervisor, unroutable from the GPU node's network. This breaks Calico's (and any pod's) access to in-cluster `Service` ClusterIPs (`kubernetes.default.svc` at `10.96.0.1`, and every other ClusterIP), since `kube-proxy`'s DNAT ultimately resolves to that same unroutable `10.0.2.2`. Real-time iptables DNAT + route + MASQUERADE patches (each individually user-approved) did get direct `10.0.2.2:6443` reachability working, but the identical problem recurs for every additional Service IP (CoreDNS, etc.) — a structural "two providers, no shared network" limitation, not a single fixable bug. Rather than keep layering ad-hoc NAT rules on a live GPU inference node (STT/LLM/TTS all serving production traffic), the join was cleanly reverted (`kubeadm reset` on the GPU node, `kubectl delete node` on the control plane, all iptables/route changes removed) — GPU services verified healthy before/during/after every step. K8S-2 (GPU taint/toleration) is validated by real Helm chart/Deployment-spec inspection (only `gpu-scheduler` requests the `gpu` node pool + tolerates the `nvidia.com/gpu` taint) and by a real, successful `kubeadm join` — but not by a fully `Ready` joined GPU node actually running a scheduled, tainted pod. **Action needed:** establish real network connectivity between the two providers (a WireGuard mesh or similar VPN between the CPU VM and GPU node, or move both to the same provider/VPC) before attempting the join again. Not blocking Sprint-026 (K8S-2 config is proven correct); relevant to any future sprint assuming a fully joined multi-node cluster. |
| TT-016 | Helm chart generator's `networkPolicy.egressPorts` only ever populates datastore ports (5432/6379), never peer-service HTTP ports — real service-to-service calls will be blocked by NetworkPolicy once business-logic HTTP APIs exist | Low | Sprint-026 (2026-07-07) | Found while proving real NetworkPolicy enforcement against the live cluster (Sprint-026 Phase 2): every chart's `values.yaml` declares `allowedNamespaces` (the *ingress* side — who may call me) but no chart's `egressPorts` lists any peer-service port (only Postgres/Redis, plus MongoDB for `analytics`) — meaning the *egress* side (who I am allowed to call) never includes another service's port. Documented cross-service calls elsewhere in this project (e.g. "PolicyEngine/Auth/Authz/AIGovernance, called synchronously on every turn," CPU_NODE_STATE.md) would be silently blocked by the current NetworkPolicy config once real HTTP entrypoints exist for those services (TT-006). Not fixed this session — doing so correctly requires the real inter-service call graph (which service calls which other service, on which port), which doesn't exist yet since every service is still a library class (TT-006) with no real caller/callee relationship over HTTP. **Action needed:** once TT-006 is resolved and services gain real business-logic HTTP APIs, extend `scripts/helm/generate_service_charts.sh`'s `SERVICES` array with each service's actual outbound call targets and regenerate `egressPorts` accordingly. Not blocking Sprint-026 (the deny-all-plus-explicit-allow-list *mechanism* is proven working — see TT-014's resolution). |
| TT-015-addendum | TT-015's apiserver-NAT limitation also blocks *in-cluster* pods from reaching `kubernetes.default.svc`, not just the GPU node | Medium | Sprint-027 (2026-07-08) | Found live while deploying the Sprint-027 observability stack: Prometheus's `kubernetes_sd_configs` jobs and FluentBit's `kubernetes` filter both time out reaching `kubernetes.default.svc:443` (ClusterIP `10.96.0.1`, DNAT'd by kube-proxy to the apiserver's advertised address `10.0.2.2`) — from pods running on the *same* control-plane node, not just the GPU node TT-015 originally documented. This is a broader instance of the same root cause (the apiserver's advertise address is NAT-internal to the hypervisor), not a new bug. Worse, FluentBit's constant 10s-interval retry against the unreachable apiserver was found to starve its own DNS resolution for its Loki output in the same process — with the `kubernetes` filter enabled, zero log records ever reached Loki even though the tail input was reading them correctly (input=114 records, output=0 delivered, confirmed via `/api/v1/metrics`). **Fixed for FluentBit:** the `kubernetes` filter was removed from `monitoring/logging/fluentbit.conf`; log lines are now parsed directly off the `cri`-parsed `log` field (no pod-metadata enrichment). **Not fixed for Prometheus:** `kubernetes-apiservers`/`kubernetes-nodes-cadvisor`/`voiceos-services` (pod-annotation SD) scrape jobs remain configured (documenting the intended architecture) but non-functional; the actually-functional path is the static `voiceos-services-static` job (`monitoring/prometheus/targets/dev.yml`), which achieved 26/26 real VoiceOS targets `up`. **Action needed:** same fix as TT-015 itself — real network connectivity (VPN/mesh) between this node and the apiserver's advertised address, or reconfiguring `kubeadm`'s `--apiserver-advertise-address` to a routable address if one becomes available. Not blocking Sprint-027 (the static-target scrape path fully satisfies the sprint's AC). |
| TT-017 | GPU node's STT/LLM/TTS inference services never bound a `/metrics` HTTP endpoint — 3 Prometheus scrape targets (`gpu-node` job) show `down` | Low | Sprint-027 (2026-07-08) | Same class of gap as TT-006 (`/health/live`/`/health/ready` implemented but never bound to a live listener) — these three GPU-side processes are real, running, and serving inference traffic (verified via their own `/health*` endpoints per `GPU_NODE_STATE.md`), they simply never integrated `prometheus_client` and exposed a `/metrics` route the way this sprint's CPU-side health-stub image now does (`deployment/k8s/health_stub/serve.py`). Not fixed this session — GPU node access requires the user's explicit approval each time (standing rule), and no GPU-side code change was requested or needed for Sprint-027's own AC (26/26 real Kubernetes-hosted VoiceOS targets `up` already satisfies "Prometheus scrapes all services"). **Action needed:** once there is a reason to touch the GPU node again, add a minimal `/metrics` route (`prometheus_client.generate_latest()`) to each of `deployment/gpu/services/{stt,tts}/server.py` and the vLLM launch config (vLLM has a built-in `/metrics` endpoint already — confirm it's reachable) — mirrors the exact pattern used for the CPU-side health-stub this sprint. Not blocking Sprint-027. |
| TT-018 | `metrics-server` is not installed anywhere in the live Kubernetes cluster — every one of the 26 HorizontalPodAutoscalers is non-functional cluster-wide | **RESOLVED** | Discovered 2026-07-08, full-repo/infra production audit; resolved same day | Live `kubectl get events -A --field-selector type=Warning` on the real cluster (`101.53.141.75`) showed `FailedComputeMetricsReplicas`/`FailedGetResourceMetric` on every single HPA — root cause confirmed via `kubectl get apiservices \| grep metrics` (empty) and `kubectl top nodes` (`error: Metrics API not available`): no `metrics-server` Deployment existed anywhere. **Fixed:** upstream `metrics-server` v0.7.2 (official image, `--kubelet-insecure-tls` since this self-managed kubeadm cluster's kubelet serving certs aren't CA-signed) added as `infra/k8s/cluster-addons/metrics-server.yaml` and applied to the live cluster (user-approved — cluster-wide RBAC/APIService). **Verified:** `kubectl top nodes`/`kubectl top pods` both return real data; 25/26 HPAs now show real `cpu: 0%/70%` targets instead of `<unknown>` (the 26th, `gpu-scheduler`, needs a *custom* metrics adapter for `voiceos_gpu_scheduler_queue_depth` — separate, already-tracked gap, same class as TT-006/TT-017, not a metrics-server problem). Actual scaling-under-load behavior still needs to be observed during Sprint-028's load test — this only proves the metrics pipeline now works, not that a real scale-out event has been seen. |
| TT-019 | Single-node, non-HA Kubernetes control plane; unexplained ~10-minute multi-pod crash-loop window this morning with no root cause captured | Medium-High | Discovered 2026-07-08, full-repo/infra production audit | Live evidence: `kube-apiserver`/`kube-controller-manager`/`kube-scheduler`/`etcd` show 3/9/8/1 restarts respectively over the cluster's 22h lifetime; `kubectl describe pod kube-apiserver-...` shows a real `Terminated`/`Error`/exit-code-1 event this morning (07:12 UTC); `journalctl -u kubelet` shows `fluentbit`/`jaeger`/`grafana`/`alertmanager` all independently entered `CrashLoopBackOff` between ~08:49–08:56 UTC the same morning, consistent with an apiserver blip cascading to anything that talks to it. Node conditions (`MemoryPressure`/`DiskPressure`/`PIDPressure`) are all `False` now and were not observed to transition during the window, and current `free -h` shows 9.6Gi available of 12Gi total — so OOM is not confirmed, but cannot be ruled out retroactively either, **because TT-018's missing metrics-server means there is no historical CPU/memory time series to diagnose it with.** Separately, this is architecturally a single control-plane node (no etcd/apiserver HA) — a structural SPOF for a cluster described elsewhere as hosting production alpha traffic. **Action needed:** once TT-018 is fixed, watch node/pod metrics under Sprint-028's load test to see if the same pattern recurs under real memory pressure; separately, decide (as an ADR, since this is architecture-level) whether a single-node control plane is acceptable for the production-alpha milestone or whether control-plane HA needs to be scoped into a sprint. Not blocking Sprint-027's already-claimed AC (which only required the stack to be `Running`, not stable under load) but should block signing off Sprint-028's chaos-engineering AC without investigation. |
| TT-020 | A real, unencrypted RSA private key (`temporary.pem`, repo root) sits inside the project working tree | **RESOLVED** | Discovered and resolved same day, 2026-07-08, full-repo/infra production audit | `temporary.pem` at the repository root was confirmed (by reading its contents) to be a genuine, complete, unencrypted `-----BEGIN RSA PRIVATE KEY-----` (39 lines) — not a placeholder or example — still actively referenced as the GPU node's SSH credential. **Fixed (user-approved):** moved to `~/.ssh/temporary.pem` (`chmod 600`, alongside `vm-node-key`/`gpu_new_key`), confirmed gone from the repo tree; updated the 3 operational doc references (`deployment/GPU_NODE_STATE.md`'s Access-key/SSH lines, `infra/dr/runbooks/gpu-node-failure.md`'s detection command, `implementation/adrs/ADR-001-vllm-tts-streaming.md`'s rsync deployment command) to the new path. `implementation/DONE.md`'s historical Sprint-011 log entry mentioning the old path was deliberately left untouched (append-only history, not an operational instruction). No `.gitignore` was needed this session since TT-021's `git init` starting point excludes it from the first commit by construction (the file no longer exists in the tree being committed). |
| TT-021 | CI/CD pipeline is well-authored but has never actually executed once | Medium — **partially resolved** | Discovered 2026-07-08, full-repo/infra production audit; git init completed same day | `.github/workflows/ci.yml` (ruff lint/format, mypy --strict, PII-log gate) and `release.yml` (Docker build+push, `helm lint`/`helm template \| kubeconform`/dry-run apply) are both present and read as reasonable, production-shaped pipelines, but neither had ever been triggered by a real GitHub Actions run (no `.git` existed at all until this audit). **Done this session (user-approved):** `git init`, a new root `.gitignore` (excludes venvs/caches/`*.pem`/`*.key`/`*_key`/`.env`/generated `*.wav`/`*.log`), `git config user.name`/`user.email` already present from a prior global config, `git add -A` + review of `git status --short` (no real secrets staged — only `.env.example` templates with `CHANGE_ME` placeholders and legitimate secrets-*management* library code, not leaked secrets), one initial commit (`ac2ec2a`) capturing the full Sprint-001–027 tree. **Not done — still open:** no remote configured and nothing pushed (deliberately, per user instruction this session — a remote/push decision needs its own explicit confirmation), so the GitHub Actions workflows still haven't executed for real yet. **Action needed:** once a remote is chosen, `git remote add origin <url>` + push, then open a throwaway PR and confirm both workflows actually go green (including that `release.yml`'s registry-credential secrets are configured in the remote's GitHub Secrets) before relying on either as a real release gate. |
| TT-022 | `src/services/user_management/sso_stub.py` docstrings say real SSO "ships in Sprint-025" — stale now that SSO/SCIM has been correctly rescoped to Sprint-032 | **RESOLVED** | Discovered and resolved same day, 2026-07-08, full-repo/infra production audit | `implementation/BACKLOG.md`'s own Epic table lists `Sprint-032 — Enterprise Platform: SSO, SCIM & Multi-Region` as the sprint that depends on Sprint-025, and `DONE.md`'s Sprint-025 entry confirms `SSOIntegration` shipped only as "an explicit Sprint-025 stub" (config-intent recording, no real SAML/OIDC handshake) — correct and expected. **Fixed:** all four stale "Sprint-025" references in `sso_stub.py` (module docstring, `SSONotImplementedError` docstring, `configure()` docstring, `authenticate()`'s raised message) updated to "Sprint-032." Confirmed no test asserts on the old string (`grep` across `tests/` for the literal message, no hits). Documentation-only change, no behavior modified. |
| TT-023 | Real payment gateway integration (Stripe/Razorpay) does not exist — `PaymentProcessor` always simulates success | Medium (blocks real revenue collection, not a bug) | Documented since Sprint-024, re-flagged 2026-07-08 during production audit | `src/services/billing/payment.py` implements `StripeGateway`/`RazorpayGateway` as explicit, honestly-labeled stubs that always return success ("no real Stripe/Razorpay account configured for this project") — consistent with the project's no-cloud-account precedent (mTLS, Vault, etc. are all self-hosted equivalents). This is correctly *not* hidden or faked as real, but it means no real money has ever moved through this system, and Sprint-028's "production alpha" cannot process real customer payments/collections outcomes until this is addressed. **Action needed:** before any pilot/production sprint that involves real collections revenue (Sprint-030 Pilot Deployment at the latest), obtain real Stripe/Razorpay (or equivalent Indian PA/PG) sandbox-then-production credentials and replace the stub gateways with real SDK integrations — this needs its own scoped sprint or explicit addition to Sprint-029/030's AC, not a silent fix. |

| TT-024 | STT service hangs GPU kernel when transcription is aborted mid-call — CUDA context becomes unrecoverable without server reboot | High | Sprint-028 (2026-07-11) | During Sprint-028 Phase 2 latency validation, killing the test client Python process mid-test left an in-flight `/transcribe` request abandoned. The STT server (ctranslate2/faster-whisper) continued running the Whisper model.encode() call in the FastAPI async handler's blocking code. The CUDA kernel entered an unrecoverable hang: (1) the STT process could not be killed even with SIGKILL (remained as a defunct zombie for 15+ minutes), (2) nvidia-smi subsequently showed `ERR!` on all sensor fields, (3) new processes could not create CUDA contexts (`RuntimeError: CUDA failed with error no CUDA-capable device is detected`), (4) existing CUDA processes (vLLM/TTS) became very slow (LLM TTFT degraded from 270ms to 6018ms, TTS timed out), (5) `nvidia-smi --gpu-reset` returned "Not Supported" on NVIDIA L4. Recovery requires a GPU server reboot. **Root causes:** (a) `async def transcribe()` calls `_model.transcribe()` (a blocking ctranslate2 call) directly in the event loop, blocking uvicorn's entire async loop — no timeout, no thread pool isolation; (b) when CUDA context gets corrupted, ctranslate2 does not have a clean recovery path, unlike PyTorch which can at least fail gracefully for new tensors. **Action needed:** (1) IMMEDIATE: reboot GPU server to restore all services (GPU server reboot was issued 2026-07-11 and services verified healthy post-reboot); (2) SPRINT-029 or earlier: run `_model.transcribe()` in a `ThreadPoolExecutor` via `asyncio.run_in_executor()` so the event loop is never blocked by transcription — this isolates CUDA kernel hangs to a background thread and lets the event loop serve health checks and reject new requests during recovery; (3) add a timeout on transcription (e.g., `concurrent.futures.wait([future], timeout=10.0)`) to detect stuck kernels; (4) add a circuit breaker on STT that marks the service unavailable if more than N consecutive requests time out. Tracked in `deployment/gpu/services/stt/server.py`. |
| TT-025 | **Sprint-028 AC-1 latency gate INCOMPLETE — first-audio p95 ≤ 1.5s never achieved under sustained load** | **Critical — blocks Sprint-028 completion and M-7** | Sprint-028 (2026-07-11) | **STATUS: BLOCKED — CPU/GPU SERVERS TEMPORARILY UNAVAILABLE.** Sprint-028 AC-1 requires first-audio p95 ≤ 1.5s over 100 sequential production calls. This gate has never been achieved under sustained load. Sprint-028 is NOT complete until this gate passes. **Current measured results (best evidence, from `evaluation/latency-validation/latency-report.md`):** Run A (Termux mobile → GPU): STT p50=344ms, LLM p50=63ms, TTS TTFA p50=728ms, **first_audio p95=2357ms — FAIL**. Run B (intra-DC CPU→GPU, production-representative): **first_audio p95=1950ms — FAIL**. Run B bimodal: calls 1–22 (cold GPU): first_audio p50≈920ms, p95≈933ms — would PASS; calls 23–100 (GPU thermal throttled after 110s continuous inference at 72W TDP limit): first_audio p50≈1900ms — FAIL. Run C (post-fix, Termux path): first_audio p50≈1981ms, min=1670ms — FAIL. **Required target: first-audio p95 ≤ 1500ms** (V1 Ch23 / Sprint-028.md AC-1). **Bottleneck:** GPU L4 thermal throttle — after ≈110s continuous inference, L4 hits 72W TDP, GPU clock drops from 2040MHz → ~1000MHz (49% of boost), all three models (STT/LLM/TTS) double in latency simultaneously. **Architectural minimum (no network overhead):** STT ~600ms + LLM ~220ms + TTS TTFA ~660ms = ~1480ms. This is within 20ms of the gate and requires: (a) fully cooled GPU (not throttled), (b) intra-DC network path (<5ms RTT), (c) no concurrent load. **Fixes already applied and deployed:** (1) TTS sliding window 28→21 tokens — TTFA baseline 856ms→640ms; (2) torch.compile removed — prevented 15,544ms catastrophic regression; (3) CUDA JIT warmup in _load_model() — prevents 1,610ms first-call spike, currently completes in 478ms; (4) orphaned synthesis thread drain — prevents multi-second queueing under sequential test load; (5) STT CUDA OOM fix — vLLM 0.55→0.45 + mandatory STT warmup transcription (323–478ms, deployed); (6) httpx keepalive retry fix — eliminates stale-socket "Server disconnected" errors. **TTS architecture budget gap (requires ADR before gate can pass):** V1 Ch23 allocates 250ms for TTS first-clause. Veena 3B BF16 + SNAC 24kHz minimum is 21 tokens × 32.7ms/tok = 642ms — physically unachievable within 250ms. Budget must be revised to ~750ms via ADR before BenchmarkSuite.run_benchmarks() can pass. **Required validation when servers return:** run `python3 scripts/validate/latency_validation_phase2.py --gpu-host 217.18.55.78 --calls 100 2>&1 \| tee /tmp/latency_run_d.txt` from CPU node (101.53.137.131) — must achieve first-audio p95 ≤ 1500ms across all 100 calls. Cold-GPU path (first 20-25 calls) passes; the gate requires the p95 across the full 100-call run to be ≤ 1500ms, which requires either: (i) a higher-TDP GPU (A10G at 150W or H100 at 400W) to avoid thermal throttling, or (ii) multiple L4 GPUs with load distribution (one node per ~25 calls before thermal throttle), per V7 Ch6 GPU fleet architecture. Servers status: GPU 217.18.55.78 and CPU 101.53.137.131 temporarily unavailable as of 2026-07-11. **Sprint-028 remains open until this gate passes.** |
| TT-026 | **Sprint-028 AC-2 load test gate INCOMPLETE — 500-concurrent-user test not executed; single L4 saturates at 10 users** | High — blocks Sprint-028 completion | Sprint-028 (2026-07-11) | Sprint-028 AC-2 requires: first-audio p95 ≤ 1.65s at 500 concurrent calls; GPU utilization ≤ 0.80; error rate < 0.1%. **Current measured results (from `evaluation/load-testing/load-test-report.md`):** 10-user concurrent test: TTS TTFA p50=12,589ms, first_audio p95=16,524ms — FAIL (10× over gate). Root cause: Veena 3B synthesis is single-threaded on the GPU — 10 concurrent requests serialize behind one synthesis queue, causing cumulative 12-13s wait. **500-user test: NOT EXECUTED.** Requires GPU fleet (V7 Ch6) — a single L4 cannot serve 500 concurrent synthesis sessions. Locust also not available on test nodes (psutil build fails on Android/Termux; pypi unreachable from CPU node). **Resolution path:** (1) provision second GPU node with identical model stack; (2) install locust on a dedicated test runner node; (3) run `tests/load/locustfile.py` with `--users 500 --spawn-rate 0.83 --run-time 45m`. |
| TT-027 | **Sprint-028 remaining open AC items — chaos GPU fleet BLOCKED; security not signed off; compliance audit gaps; canary not executed** | High — blocks Sprint-028 / M-7 | Sprint-028 (2026-07-11) | The following Sprint-028 AC items remain FAIL or UNVALIDATED beyond TT-025/TT-026: **(a) AC-3 Chaos — GPU fleet failover BLOCKED:** kill-GPU-node-0 scenario cannot execute (single L4 node, TT-015 prevents fleet joining); required: second GPU node + load balancer + GPU scheduler active. **(b) AC-4 Security — HIGH findings conditional; pen test not signed off:** PEN-001/002/003 (unauthenticated inference endpoints STT/LLM/TTS) are HIGH findings not exploitable only if API gateway deployed before external exposure — currently exploitable in staging; pen test report explicitly "CONDITIONAL NO-GO" and not signed off by engineering lead; PEN-004 (prompt injection) requires dedicated red-team (Garak/PyRIT) follow-up; PEN-005-007 (TTS speaker field no allowlist) is a fixable code change (`ALLOWED_SPEAKERS = frozenset({"kavya"})`, return 422 on unknown speaker). **(c) AC-5 Compliance — audit infrastructure gaps:** audit hash chain not fully populated for live writes (some NULL hashes on recent entries — hash computation wired only to seeding flow, not live event writes); PolicyEngine.evaluate() does not write to audit_log on every policy decision (V4 Ch11 requires all ALLOW and DENY decisions logged). **(d) AC-6 Canary — NOT EXECUTED:** no Argo Rollouts, no Flagger, no weighted ingress deployed; FleetRolloutManager is Python ring-assignment only, not K8s traffic splitting; 5%→25%→50%→100% canary cannot execute without this infrastructure. **(e) AC-8 BenchmarkSuite.run_benchmarks() FAIL:** TTS 250ms budget vs 642ms physical minimum — ADR required to revise V1 Ch23 TTS budget to 750ms before this AC can pass. **(f) AC-12 Threat model not signed off:** docs/security/threat-model.md exists and has all 6 STRIDE categories (verified), but AC requires "reviewed and signed off by engineering lead" — this has not occurred. **Resolution order:** sign-off on threat model → fix PEN-005-007 speaker allowlist → fix audit hash chain + policy decision logging → deploy API gateway (resolves PEN-001-003) → TTS ADR (resolves AC-8) → GPU fleet (resolves AC-2 load test + AC-3 chaos) → re-run latency gate (resolves AC-1) → canary (resolves AC-6) → final sign-off. |
| TT-028 | **TCP MSS clamp for CPU→GPU traffic not persisted across a CPU node reboot** | Medium — any real call will hang on GPU inference again if the CPU node restarts | Path-A Runtime Consolidation Phase 7 (2026-07-25) | **RESOLVED same day.** Path-A Phase 7's live dry run found a Path MTU black-hole on the CPU→GPU network path: TCP connects and returns HTTP 200 fine, but a sustained streamed response (real TTS synthesis, a real LLM completion) never delivers any body bytes even after a 5-minute client timeout — small/instant responses (health checks) work fine. This is the first code path in the project's history to exercise sustained CPU→GPU streaming inference traffic (Call-001 ran entirely on the GPU node itself via Twilio-native STT/TTS — see CHANGELOG.md's Phase 7 entry — so it never could have hit this). Initially fixed with a manual `iptables` rule that didn't survive a reboot. `apt install iptables-persistent` was attempted but rejected: the Ubuntu mirror connection failed transiently mid-install, and the package's own dependency resolution would have **removed `ufw`** as a side effect (harmless here since `ufw` was already inactive, but an unjustified new-package footprint for one rule — CLAUDE.md's Dependency Policy). **Fixed properly instead** with a new self-contained systemd oneshot unit, `/etc/systemd/system/voiceos-gpu-mss-clamp.service` (not tracked in this repo — provisioned directly on the CPU node; a future `deployment/cpu/` provisioning script should adopt it), `enable --now`'d, idempotent (`iptables -C` check before `-A`). **Verified against a real reboot, not assumed:** the CPU node was actually rebooted (`sudo reboot`, confirmed via `uptime` showing `up 0 min` post-reconnect); after reboot, the systemd unit was `enabled`/`active`, the TCPMSS rule was present in `iptables -t mangle -L OUTPUT`, Postgres/Redis both came back healthy, and a real TTS synthesis call to the GPU node succeeded (200/120,896 bytes/3.4s) — full end-to-end confirmation, not just rule-presence. |

---

## Milestone Tracker

| Milestone | Sprint | Status |
|---|---|---|
| Foundation Complete | Sprint-003 | ✅ **Complete** (2026-06-30) |
| Core Runtime Complete | Sprint-008 | ✅ **Complete** (2026-06-30) |
| Walking Skeleton (M-3) | Sprint-012 | ✅ **Reached** (2026-07-03) — pipeline proven; Phase 3 streaming fix (2026-07-04): TTS server p95=873ms ✅; pipeline p50=2355ms p95=6048ms (TT-001-residual) |
| Reliability Complete (M-4) | Sprint-016 | ✅ **Reached** (2026-07-04) — CircuitBreaker/BoundedQueue/Health/Observability validated against real Postgres+Redis |
| Compliance & Security Complete | Sprint-020 | ✅ **Reached** (2026-07-05) — PII protection, hash-chained audit trail, API/runtime security, AI safety, ComplianceMonitoring, IncidentResponse all validated against real Postgres/Redis on the CPU node |
| SaaS Platform Complete | Sprint-025 | ✅ **Reached** (2026-07-06) |
| Production Alpha | Sprint-028 | ⬜ Pending |
| Founder Validation | Sprint-029 | ⬜ Pending |
| Pilot Deployment | Sprint-030 | ⬜ Pending |
| Enterprise Platform Complete | Sprint-032 | ⬜ Pending |
| Workflow & Customer Success Complete | Sprint-033 | ⬜ Pending |
| Learning Layer Complete | Sprint-034 | ⬜ Pending |
| Production Release | Sprint-031 | ⬜ Pending |
